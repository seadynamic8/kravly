<div class="row">
	<div class="large-10 large-centered columns">
		<%= form_for @idea, html: { class: "custom" } do |f| %>
			<fieldset>
				<legend>Add An New Idea</legend>

				<%= render partial: 'form', locals: { f: f } %>
			</fieldset>
		<% end %>
	</div>
</div>