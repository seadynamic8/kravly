<% @comment.body = "Edit: " + @comment.body %>
<%= form_for @comment, remote: true, html: { class: "comment-edit-form" } do |f| %>
	<%= f.text_area :body, label: false %>
	<%= f.submit class: "small button", id: "update-button" %>
<% end %>